{
  "name": "graph-cfi-staging",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ ammarsjw/graph-cfi-staging",
    "create-local": "graph create --node http://localhost:8020/ ammarsjw/graph-cfi-staging",
    "remove-local": "graph remove --node http://localhost:8020/ ammarsjw/graph-cfi-staging",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ammarsjw/graph-cfi-staging",
    "test": "graph test",
    "codegen-build": "graph codegen && graph build",
    "deploy-hosted-service": "graph deploy --product hosted-service ammarsjw/graph-cfi-staging"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.61.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
